---
phase: 04-ux-polish-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prospeqt-email-qa/app/templates/base.html
  - prospeqt-email-qa/app/templates/dashboard.html
  - prospeqt-email-qa/app/templates/workspace.html
  - prospeqt-email-qa/app/templates/campaign.html
  - prospeqt-email-qa/app/templates/login.html
  - prospeqt-email-qa/app/templates/admin.html
  - prospeqt-email-qa/app/routes/dashboard.py
  - prospeqt-email-qa/qa/screenshots/.gitkeep
autonomous: true
requirements:
  - UX-01
  - UX-05
user_setup: []

must_haves:
  truths:
    - "All 6 templates use consistent transition timing of 200ms ease (not 0.15s)"
    - "All interactive elements (buttons, cards, table rows, links) have visible hover states"
    - "Typography hierarchy is clear: headings use font-weight 600-700, body 400, meta/labels 500"
    - "8px spacing grid is consistent across card padding, gaps, and page shell"
    - "Health check endpoint returns JSON at /health"
    - "UX design expert agent has audited the templates and no critical structural issues remain"
  artifacts:
    - path: "prospeqt-email-qa/app/templates/base.html"
      provides: "Refined CSS design system tokens and shared component styles"
      contains: "transition.*0\\.2s ease"
    - path: "prospeqt-email-qa/qa/screenshots/.gitkeep"
      provides: "QA screenshot output directory"
  key_links:
    - from: "prospeqt-email-qa/app/templates/base.html"
      to: "all 5 child templates"
      via: "Jinja2 extends and CSS custom properties"
      pattern: "extends.*base\\.html"
---

<objective>
Polish the visual design system across all 6 templates, add a health check endpoint, and run the UX design expert agent for evidence-based review.

Purpose: Achieve the luxury aesthetic standard (UX-01) with UX expert validation (UX-05). CSS refinements propagate through base.html inheritance to all templates.
Output: Polished templates, /health endpoint, QA screenshot directory, UX expert audit passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ux-polish-deployment/04-CONTEXT.md
@.planning/phases/04-ux-polish-deployment/04-RESEARCH.md

@prospeqt-email-qa/app/templates/base.html
@prospeqt-email-qa/app/templates/dashboard.html
@prospeqt-email-qa/app/templates/workspace.html
@prospeqt-email-qa/app/templates/campaign.html
@prospeqt-email-qa/app/templates/login.html
@prospeqt-email-qa/app/templates/admin.html
@prospeqt-email-qa/app/routes/dashboard.py
@prospeqt-email-qa/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS design system refinement + health endpoint + QA directory</name>
  <files>
    prospeqt-email-qa/app/templates/base.html,
    prospeqt-email-qa/app/templates/dashboard.html,
    prospeqt-email-qa/app/templates/workspace.html,
    prospeqt-email-qa/app/templates/campaign.html,
    prospeqt-email-qa/app/templates/login.html,
    prospeqt-email-qa/app/templates/admin.html,
    prospeqt-email-qa/app/routes/dashboard.py,
    prospeqt-email-qa/qa/screenshots/.gitkeep
  </files>
  <read_first>
    prospeqt-email-qa/app/templates/base.html,
    prospeqt-email-qa/app/templates/dashboard.html,
    prospeqt-email-qa/app/templates/workspace.html,
    prospeqt-email-qa/app/templates/campaign.html,
    prospeqt-email-qa/app/templates/login.html,
    prospeqt-email-qa/app/templates/admin.html,
    prospeqt-email-qa/app/routes/dashboard.py,
    prospeqt-email-qa/app/main.py
  </read_first>
  <action>
    Per D-01: Refine the current design system — do not overhaul. Specific changes:

    **base.html CSS custom properties — add/update these tokens:**
    - Add `--sh-lg: 0 8px 24px rgba(0,0,0,0.12);` for elevated states
    - Add `--radius: 8px;` and `--radius-lg: 12px;` for consistent border-radius
    - Add `--transition: 0.2s ease;` as a shared transition token
    - Keep all existing color tokens unchanged

    **base.html transition consistency (per D-01: 200-300ms eases):**
    - Change ALL `transition: ... 0.15s` to use `var(--transition)` or `0.2s ease`
    - Specifically update: `.gear-btn` (line ~87-88), `.scan-btn-primary` (line ~166), `.scan-btn-secondary` (line ~186)
    - Apply transition to `.ws-card` hover if present in dashboard.html
    - Apply transition to table row hover states if present in workspace.html/campaign.html

    **Typography weight hierarchy refinement:**
    - Confirm headings (h1, h2, h3 or equivalent class) use font-weight 600 or 700
    - Body text stays at 400
    - Labels, meta text, badge text at 500
    - Add a `.text-meta` utility class: `font-size: 12px; color: var(--tx3); font-weight: 500; letter-spacing: 0.01em;`

    **Hover state coverage:**
    - Ensure table rows in workspace.html and campaign.html have `tr:hover { background: var(--bg-hov); }` with transition
    - Ensure workspace cards in dashboard.html have hover shadow elevation: `box-shadow: var(--sh-md)` on hover (from `var(--sh)` default)
    - Ensure all `<a>` tags inside content areas have hover underline or color shift

    **Spacing consistency (8px grid):**
    - `.page-shell` padding: 24px (3 * 8px) — keep as-is
    - Card internal padding: 16px or 24px (2 or 3 * 8px)
    - Gap between cards/elements: 16px (2 * 8px)
    - Verify these values in dashboard.html, workspace.html, campaign.html and fix any that deviate

    **Per-page template refinements (read each template first to find specifics):**
    - dashboard.html: Ensure workspace cards have consistent padding, shadow, border-radius using the shared tokens
    - workspace.html: Ensure campaign table has hover states, consistent cell padding
    - campaign.html: Ensure variable summary cards and broken leads table are styled consistently
    - login.html: Center the login form, add a subtle card shadow `var(--sh-md)`, ensure input focus states are visible (outline or border color change to `var(--blue)`)
    - admin.html: Ensure workspace list items have hover states, action buttons use shared button styles

    **Health check endpoint — add to dashboard.py:**
    ```python
    @router.get("/health")
    async def health():
        return {"status": "ok"}
    ```

    **QA directory — create:**
    - `prospeqt-email-qa/qa/screenshots/.gitkeep` (empty file to track the directory in git)
  </action>
  <verify>
    <automated>cd /Users/mihajlo/Desktop/claude-code/prospeqt-email-qa && .venv/bin/python -m pytest tests/ -x -q && grep -c "0\.15s" app/templates/base.html | grep -q "^0$" && grep -q "0\.2s ease" app/templates/base.html && grep -q '"/health"' app/routes/dashboard.py && test -f qa/screenshots/.gitkeep && echo "ALL CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - base.html contains `--sh-lg:` custom property
    - base.html contains `--transition: 0.2s ease` custom property
    - base.html contains ZERO occurrences of `0.15s` (all replaced with 0.2s ease or var(--transition))
    - base.html contains `--radius: 8px` and `--radius-lg: 12px` custom properties
    - dashboard.py contains `@router.get("/health")` and `return {"status": "ok"}`
    - login.html contains `var(--sh-md)` or `var(--sh-lg)` for the form card
    - workspace.html contains `tr:hover` or table row hover styling
    - campaign.html contains hover state styling for table rows or cards
    - `qa/screenshots/.gitkeep` exists
    - `python -m pytest tests/ -x -q` exits 0 (no regressions)
  </acceptance_criteria>
  <done>
    All 6 templates refined with consistent transitions (0.2s ease), hover states on all interactive elements, 8px spacing grid, typography weight hierarchy. /health endpoint returns 200 with {"status": "ok"}. QA screenshot directory exists. All existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: UX design expert agent audit and iteration</name>
  <files>
    prospeqt-email-qa/app/templates/base.html,
    prospeqt-email-qa/app/templates/dashboard.html,
    prospeqt-email-qa/app/templates/workspace.html,
    prospeqt-email-qa/app/templates/campaign.html,
    prospeqt-email-qa/app/templates/login.html,
    prospeqt-email-qa/app/templates/admin.html
  </files>
  <read_first>
    prospeqt-email-qa/app/templates/base.html,
    prospeqt-email-qa/app/templates/dashboard.html,
    prospeqt-email-qa/app/templates/workspace.html,
    prospeqt-email-qa/app/templates/campaign.html,
    .claude/agents/ux-design-expert.md
  </read_first>
  <action>
    Per D-12: Run the `ux-design-expert` agent as a post-implementation gate after Task 1's CSS work.
    Per D-05: Screenshots validated by the ux-design-expert agent — no manual human review step required.
    Per D-13: UX expert agent takes Playwright screenshots, checks all 3 viewports, and clicks through the UI.
    Per D-14: Stop iterating when fixes hit diminishing returns (cosmetic, not structural).

    **Process:**
    1. Start the FastAPI dev server: `cd prospeqt-email-qa && .venv/bin/python -m uvicorn app.main:app --reload --port 8000`
    2. Use the `playwright-cli-expert` agent to take screenshots at 3 viewports (1440x900, 768x1024, 375x812) of the overview page (/)
    3. Spawn the `ux-design-expert` agent with:
       - All 6 template source files as context
       - The Playwright screenshots as visual input
       - Instruction to evaluate across 8 dimensions and produce Top 5 Highest-Impact Fixes
    4. Apply the structural fixes recommended by the UX expert (skip cosmetic-only fixes per D-14)
    5. Re-run Playwright screenshots to verify fixes
    6. If UX expert flagged critical issues, re-run audit. Stop when grade is B or above on all structural dimensions.

    **UX expert agent scoring threshold (from UX-01 success criteria):**
    - Pass = No critical design issues outstanding
    - Critical = issues with visual hierarchy, data hierarchy, touch targets, contrast, or information architecture
    - Cosmetic = shadow shade tweaks, 1-2px spacing adjustments, font weight micro-adjustments
  </action>
  <verify>
    <automated>cd /Users/mihajlo/Desktop/claude-code/prospeqt-email-qa && .venv/bin/python -m pytest tests/ -x -q && echo "REGRESSION CHECK PASS"</automated>
  </verify>
  <acceptance_criteria>
    - UX design expert agent has been run at least once (confirmed by agent audit output in execution log)
    - No critical structural issues remain after iteration (visual hierarchy, data hierarchy, touch targets, contrast)
    - All existing tests still pass (`python -m pytest tests/ -x -q` exits 0)
    - Templates still extend base.html (no broken inheritance)
    - All CSS custom properties still defined in base.html :root block
  </acceptance_criteria>
  <done>
    UX design expert agent has audited all templates. All critical structural issues resolved. Remaining issues are cosmetic only (per D-14 stop rule). Templates are visually polished to luxury standard (UX-01). Agent consulted as required by UX-05.
  </done>
</task>

</tasks>

<verification>
1. `cd prospeqt-email-qa && .venv/bin/python -m pytest tests/ -x -q` — all existing tests pass
2. `grep -c "0\.15s" prospeqt-email-qa/app/templates/base.html` returns 0
3. `curl -s http://localhost:8000/health` returns `{"status":"ok"}`
4. UX design expert agent audit completed with no critical issues
</verification>

<success_criteria>
- Dashboard has polished, professional visual design with consistent transitions, hover states, typography hierarchy, and spacing grid
- UX design expert agent consulted and passed (no critical structural issues)
- /health endpoint available for Render health checks
- QA screenshot directory created for Playwright output
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-ux-polish-deployment/04-01-SUMMARY.md`
</output>
