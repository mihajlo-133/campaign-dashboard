---
phase: 01-api-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - prospeqt-email-qa/app/api/instantly.py
  - prospeqt-email-qa/app/models/instantly.py
  - prospeqt-email-qa/tests/test_instantly.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
  - API-07

must_haves:
  truths:
    - "API client fetches all campaigns from a workspace using cursor pagination"
    - "Only draft (status=0) and active (status=1) campaigns are returned — paused and completed are filtered out"
    - "Sequence copy is extracted from inline campaign.sequences[].steps[].variants[].body/subject — no extra API call"
    - "All active leads (status=1) are fetched via POST /leads/list with cursor pagination — no leads missed"
    - "Lead variables are read from lead.payload dict, not custom_variables"
    - "Per-workspace rate limiting prevents 429 errors"
  artifacts:
    - path: "prospeqt-email-qa/app/api/instantly.py"
      provides: "Async Instantly v2 API client: list_campaigns, fetch_all_leads, extract_copy_from_campaign"
      exports: ["list_campaigns", "fetch_all_leads", "extract_copy_from_campaign"]
    - path: "prospeqt-email-qa/app/models/instantly.py"
      provides: "Pydantic models: Campaign, Lead, Workspace"
    - path: "prospeqt-email-qa/tests/test_instantly.py"
      provides: "API client tests using respx mocks — covers pagination, filtering, rate limiting"
  key_links:
    - from: "prospeqt-email-qa/app/api/instantly.py"
      to: "https://api.instantly.ai/api/v2"
      via: "httpx.AsyncClient with Bearer auth"
      pattern: "INSTANTLY_BASE.*api.instantly.ai"
    - from: "prospeqt-email-qa/app/api/instantly.py"
      to: "prospeqt-email-qa/app/services/workspace.py"
      via: "gets API key from workspace registry"
      pattern: "get_api_key"
---

<objective>
Build the async Instantly v2 API client with cursor pagination for campaigns and leads, status filtering, inline copy extraction, and per-workspace rate limiting.

Purpose: This is the data fetching layer — the QA engine (Phase 2) and dashboard views (Phase 3) consume its output. Getting pagination and filtering right here prevents false negatives downstream.
Output: `app/api/instantly.py` with full test coverage via respx mocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-api-foundation/01-CONTEXT.md
@.planning/phases/01-api-foundation/01-RESEARCH.md
@.planning/phases/01-api-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — workspace registry interface the API client uses -->
From app/services/workspace.py:
```python
def get_api_key(name: str) -> str | None
def list_workspaces() -> list[dict]  # [{"name": "enavra", "key_preview": "...4"}]
```

From app/config.py:
```python
class Settings(BaseSettings):
    request_timeout: int = 15
```

From tests/conftest.py:
```python
# Available fixtures: mock_env, app_client, campaign_response, leads_response, fixtures_dir
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Pydantic models and Instantly API client with pagination + filtering</name>
  <files>
    prospeqt-email-qa/app/models/instantly.py,
    prospeqt-email-qa/app/api/instantly.py,
    prospeqt-email-qa/tests/test_instantly.py
  </files>
  <read_first>
    prospeqt-email-qa/app/services/workspace.py,
    prospeqt-email-qa/app/config.py,
    prospeqt-email-qa/tests/conftest.py,
    prospeqt-email-qa/tests/fixtures/campaign_response.json,
    prospeqt-email-qa/tests/fixtures/leads_response.json,
    .planning/phases/01-api-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    - test_list_campaigns_returns_all_pages: Mock 2 pages of campaigns (page 1 returns next_starting_after cursor, page 2 returns null cursor). Verify all campaigns from both pages are collected.
    - test_campaign_status_filter: Given campaigns with statuses 0, 1, 2, 3 — only status 0 and 1 are returned.
    - test_extract_campaign_copy: Given a campaign with inline sequences, extract_copy_from_campaign returns all subject + body text concatenated.
    - test_fetch_all_leads_pagination: Mock 2 pages of leads (first page has cursor, second has null). Verify all leads collected.
    - test_lead_status_filter: Given leads with statuses 1, 3, -1 — only status=1 leads are returned.
    - test_lead_payload_read: Verify lead["payload"] dict is preserved (not lead["custom_variables"]).
    - test_rate_limit_semaphore: Verify per-workspace semaphore limits concurrent requests (mock 10 concurrent calls, verify max 5 in-flight at once using an asyncio counter).
    - test_api_error_raises: When API returns 500, httpx.HTTPStatusError is raised.
  </behavior>
  <action>
    1. **app/models/instantly.py** — Pydantic v2 models:
       ```python
       from pydantic import BaseModel

       class LeadPayload(BaseModel):
           """Lead variables from lead.payload dict. Flexible — any string keys."""
           model_config = {"extra": "allow"}

       class Lead(BaseModel):
           id: str
           email: str
           status: int
           payload: dict[str, str | None] = {}  # Lead variables — key is var name, value is var value

       class CampaignVariant(BaseModel):
           subject: str = ""
           body: str = ""

       class CampaignStep(BaseModel):
           variants: list[CampaignVariant] = []

       class CampaignSequence(BaseModel):
           steps: list[CampaignStep] = []

       class Campaign(BaseModel):
           id: str
           name: str
           status: int
           sequences: list[CampaignSequence] = []
       ```

    2. **app/api/instantly.py** — Async API client:
       - `INSTANTLY_BASE = "https://api.instantly.ai/api/v2"`
       - Module-level `_semaphores: dict[str, asyncio.Semaphore] = {}` for per-workspace rate limiting
       - `_get_semaphore(workspace_name: str) -> asyncio.Semaphore`: Creates Semaphore(5) per workspace on first access
       - `async def list_campaigns(client: httpx.AsyncClient, api_key: str, workspace_name: str = "default") -> list[dict]`:
         - Uses Bearer auth header: `{"Authorization": f"Bearer {api_key}"}`
         - GET `{INSTANTLY_BASE}/campaigns` with `{"limit": 100}`
         - Cursor pagination: loop until `next_starting_after` is None or items < 100
         - Filter result: `[c for c in all_campaigns if c.get("status") in (0, 1)]`
         - Rate limit: `async with _get_semaphore(workspace_name):`
         - 100ms sleep between pages: `await asyncio.sleep(0.1)`
       - `async def fetch_all_leads(client: httpx.AsyncClient, api_key: str, campaign_id: str, workspace_name: str = "default") -> list[dict]`:
         - POST `{INSTANTLY_BASE}/leads/list` with body `{"campaign": campaign_id, "limit": 100}`
         - Cursor pagination via `starting_after` in POST body
         - Filter: only keep leads where `lead.get("status") == 1`
         - Rate limit: `async with _get_semaphore(workspace_name):`
         - 100ms sleep between pages
         - Comment: `# Lead variables are in lead["payload"] — verified 2026-04-04`
       - `def extract_copy_from_campaign(campaign: dict) -> list[dict]`:
         - Iterates `campaign["sequences"][*]["steps"][*]["variants"]`
         - Returns list of `{"subject": str, "body": str}` for each variant
         - No additional API call — copy is inline (per API-03)

    3. **tests/test_instantly.py** — Tests using respx mocks:
       - Use `respx.mock` decorator/context manager
       - Mock GET `https://api.instantly.ai/api/v2/campaigns` with fixture data
       - Mock POST `https://api.instantly.ai/api/v2/leads/list` with fixture data
       - For pagination tests: mock first call returning `next_starting_after: "cursor-1"`, second call returning `next_starting_after: null`
       - For rate limit test: use `asyncio.gather` to fire 10 concurrent `list_campaigns` calls, instrument the semaphore to verify max 5 concurrent
       - All tests use `httpx.AsyncClient()` (no ASGI transport needed — respx intercepts at transport level)
  </action>
  <verify>
    <automated>cd prospeqt-email-qa && source .venv/bin/activate && pytest tests/test_instantly.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/instantly.py` contains `INSTANTLY_BASE = "https://api.instantly.ai/api/v2"`
    - `app/api/instantly.py` contains `async def list_campaigns` with cursor pagination loop and `status in (0, 1)` filter
    - `app/api/instantly.py` contains `async def fetch_all_leads` with POST method, cursor pagination, and `status == 1` filter
    - `app/api/instantly.py` contains `def extract_copy_from_campaign` iterating sequences.steps.variants
    - `app/api/instantly.py` contains `_get_semaphore` returning `asyncio.Semaphore(5)` per workspace
    - `app/api/instantly.py` contains comment `# Lead variables are in lead["payload"]`
    - `app/api/instantly.py` contains `await asyncio.sleep(0.1)` between paginated requests
    - `app/models/instantly.py` contains `class Campaign`, `class Lead` with `payload: dict`
    - `tests/test_instantly.py` has at least 7 test functions covering pagination, filtering, copy extraction, rate limiting
    - All tests pass: `pytest tests/test_instantly.py` exits 0
  </acceptance_criteria>
  <done>Instantly API client fetches campaigns (GET, cursor pagination, status 0+1 filter), leads (POST, cursor pagination, status=1 filter), extracts inline copy, rate-limits per workspace — all verified by passing respx-mocked tests</done>
</task>

</tasks>

<verification>
1. `cd prospeqt-email-qa && source .venv/bin/activate && pytest tests/test_instantly.py -v` — all API client tests pass
2. `grep -c "async def" prospeqt-email-qa/app/api/instantly.py` — at least 2 async functions (list_campaigns, fetch_all_leads)
3. `grep "status in (0, 1)" prospeqt-email-qa/app/api/instantly.py` — campaign filter present
4. `grep 'status.*== 1' prospeqt-email-qa/app/api/instantly.py` — lead filter present
5. `grep "Semaphore" prospeqt-email-qa/app/api/instantly.py` — rate limiting present
6. `grep "payload" prospeqt-email-qa/app/api/instantly.py` — correct field name referenced
</verification>

<success_criteria>
- All 8 test functions in test_instantly.py pass
- API client handles multi-page pagination for both campaigns (GET) and leads (POST)
- Campaign status filter only returns status 0 and 1
- Lead status filter only returns status 1
- Copy extraction works on inline sequences without extra API calls
- Per-workspace semaphore(5) prevents concurrent request flooding
- Models use `payload` dict for lead variables (not custom_variables)
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-foundation/01-02-SUMMARY.md`
</output>
